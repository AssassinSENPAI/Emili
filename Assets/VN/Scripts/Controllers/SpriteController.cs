using UnityEngine;

public class SpriteController : MonoBehaviour
{
    // at the start of the game, every sprite is hidden
    // main character Emili is always shown at the left of the screen and the other characters are shown at the right of the screen
    // When the right character is speaking, the sprite will be shown
    // When another character speaks, the sprite will change to the new character
    // I can choose to hide the sprite whenever I want
    // Every sprite has a hide and show animation
    // And every sprite has different animations for different emotions
    // there can only be two sprites shown at the same time, Emili and the other character



    }
